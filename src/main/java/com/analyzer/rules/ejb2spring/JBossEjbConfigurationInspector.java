package com.analyzer.rules.ejb2spring;

import com.analyzer.core.InspectorDependencies;
import com.analyzer.core.InspectorResult;
import com.analyzer.core.ProjectFile;
import com.analyzer.core.RequiredTags;
import com.analyzer.inspectors.core.source.AbstractSourceFileInspector;
import org.w3c.dom.Document;[ERROR]Failed to process response:Too many requests,please wait before trying again.You have sent too many requests.Wait before trying again.
